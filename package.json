{
  "name": "best-list-bay",
  "version": "1.0.0",
  "description": "Discover the best products, books, and gear worth your money",
  "main": "index.html",
  "scripts": {
    "dev": "live-server --port=3000 --open=/",
    "start": "live-server --port=3000 --open=/",
    "build": "mkdir -p public && cp -r index.html images robots.txt sitemap.xml public/",
    "serve": "live-server --port=8080"
  },
  "keywords": ["best-products", "reviews", "coffee", "audiobooks", "static-site"],
  "author": "Best List Bay",
  "license": "MIT",
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
